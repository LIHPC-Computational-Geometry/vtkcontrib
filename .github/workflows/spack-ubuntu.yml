name: Spack/Ubuntu

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call:
    name: Call lihpccg Spack/Ubuntu
    uses: LIHPC-Computational-Geometry/lihpccg-ci/.github/workflows/spack-ubuntu.yml@main
    with:
      run-tests: false
      cmake-options: "-DVTK_7:BOOL=ON -DUSE_OPENGL_BACKEND:BOOL=ON"
